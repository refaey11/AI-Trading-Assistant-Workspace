# Memory Shadow Bridge V1 — Runtime Gate — 2026-08-26

## Verified inputs
- Shadow bridge implementation exists for chronological development years 2016–2024.
- Historical Context, Historical Outcome, Similarity, and Context-Aware Retrieval are packaged as evidence only.
- 2025 and future evidence fail closed.
- The recovered Decision Brain is not modified by the bridge.

## CI evidence
The CircleCI screenshot supplied during this session shows the new `test: add pre-2025 memory shadow...` run passing, and the `feat: add shadow-only memory e...` run passing. This validates the compatibility layer and its CI wiring as executed in the current project pipeline view.

## Existing runtime evidence
Run 070 already demonstrated a real pre-2025 historical-memory integration smoke test using project artifacts: 20 similarity candidates returned, 17 outcome-eligible cases, 100% context coverage, no 2025 calibration leakage, and no similarity-driven direction change.

## Current boundary
The current 2025 producer still does not consume the shadow package as governed decision evidence. The next promotion gate is therefore a pre-2025 consumption test against the current handoff adapter, not a 2025 profitability run.

## Required checks before promotion
1. Memory evidence reaches the `historical_evidence` envelope.
2. Latest evidence timestamp is before query timestamp.
3. No 2025/future evidence enters development mode.
4. Decision direction is invariant with memory attached.
5. Similarity predicted return is never passed into the recovered Brain direction path.
6. Memory remains evidence-only and cannot produce a final trade decision.

## OOS governance
2025 remains locked and is not used for memory tuning, calibration, threshold selection, or semantic changes.
