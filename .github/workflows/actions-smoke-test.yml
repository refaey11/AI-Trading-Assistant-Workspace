name: Actions Smoke Test (MANUAL)

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm GitHub Actions is running
        run: echo "ACTIONS_SMOKE_TEST_OK"
