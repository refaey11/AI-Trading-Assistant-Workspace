name: Current Stack Development Backtest 2016-2024 (Deprecated Shim)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Use V2 governed workflow
        run: |
          echo "This workflow is deprecated."
          echo "Use current-stack-development-backtest-2016-2024-v2.yml only."
          echo "The V2 workflow owns Component Completeness, Wiring, governed source resolution, memory wiring, and the 2016-2024 replay."