name: Overnight 0006-0007 Runner (DISABLED)

# Archived diagnostic workflow. Manual-only to prevent accidental runs.
# Use governed-integration-gate-2026-08-28.yml for the official project path.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived diagnostic workflow; no automatic execution.'
