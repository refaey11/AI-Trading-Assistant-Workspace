name: Similarity Isolation Governance V1

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  similarity_isolation_governance_v1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test dependencies
        run: python -m pip install --upgrade pip pytest
      - name: Run Similarity direction-isolation regression
        run: python -m pytest -q tests/compatibility/test_similarity_isolation_governance_v1.py
