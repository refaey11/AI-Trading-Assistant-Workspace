# Murphy 0006/0007 Checkpoint V2 — 2026-08-13

## Current state

The compatibility audit has now been continued against the assembled Workspace artifacts and GitHub branches.

### Confirmed
- PIVOT_SEQUENCE_V2 is reusable and supplies confirmed pivots + availability chronology.
- TRENDLINE_GEOMETRY_V1 is reusable and supplies anchors, line family, direction, slope, and line availability.
- Geometry explicitly excludes breakout detection and thresholds.
- Candidate evidence V4 is QA-clean for 2016–2024: 166 x 0006, 181 x 0007, 347 total, candidate-only.
- Murphy Chapter 4 source contract resolves the qualitative third-test semantics: third successful touch + bounce confirms validity.
- Existing 0006/0007 evaluator architecture is treated as evaluator-side logic that should consume upstream facts rather than rebuild geometry.

### Newly implemented
`src/murphy_0006_0007/upstream_fact_adapter.py`

This adapter only promotes already-recorded candidate observations:
- third_touch candidate = same-family candidate pivot + existing daily-range/line intersection observation;
- reaction_bounce candidate = existing directional-consistency observation;
- no_break = unknown;
- confirmation_available_timestamp = unknown.

No tolerance, ATR, percentage, pip, lookback, or timeframe was invented.

Tests added:
`tests/test_murphy_0006_0007_upstream_fact_adapter.py`

CI workflow updated to run the new test.

## Critical limitation

The adapter deliberately does NOT claim that the candidate observations are the final Murphy production predicates. In particular:
- daily-range intersection is an existing evidence observation, not a newly invented touch tolerance;
- directional consistency is existing evidence, not a newly invented reaction magnitude rule;
- no-break remains unresolved;
- confirmation availability remains unresolved until the successful bounce is known from completed data.

Therefore the production evaluator remains `NOT_YET_EVALUABLE`.

## No-break result

GitHub/source search found no approved 0006/0007-specific executable no-break predicate. `break_structure_up/down` is not reused as a 0006/0007 operator. Murphy's general 3% / 2-consecutive-day examples remain unbound.

## Next exact action

Do NOT tune historical data and do NOT build another evaluator.

Next, inspect whether the existing candidate-data producer can expose the completed-bar timestamp at which the directional reaction becomes known. If that field already exists, adapt it without inventing a new lookback. Then perform the same evidence-only treatment for no-break; if no authoritative no-break fact exists, keep it `UNKNOWN` and production remains blocked.

2025 remains OOS.
